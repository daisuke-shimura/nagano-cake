<h3><%= @customer.last_name %> <%= @customer.first_name %>さんのマイページ</h3>

<p>名前：<%= @customer.last_name %> <%= @customer.first_name %></p>
<p>メールアドレス：<%= @customer.email %></p>
<p>会員ステータス：
  <% if @customer.is_active == true %>
    有効
  <% else %>
    退会
  <% end %>
</p>
<%= link_to "編集", edit_customer_path(@customer.id) %>
<%= link_to "カート", cart_items_path %>
<%= link_to "注文", new_order_path %>
<%= link_to "退会", customer_confirm_path(@customer.id) %>
<%= link_to "配送先の登録", address_index_path %>