<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/d81676dda2.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="container">
        <div class="row">
          <% if request.path.include?("/admin") %>
            <!-- 管理者用ヘッター -->
            <div class="col">
              <h2><i class="fa-solid fa-user-tag"></i>管理者用ページ</h2>
            </div>
            <div class="col">
              <% if admin_signed_in? %>
                <ul>
                  <li><%= link_to "ログアウト", destroy_admin_session_path, method: :delete %></li>
                </ul>
              <% else %>
                <ul>
                  <li><%= link_to "ログイン", new_admin_session_path %></li>
                </ul>
              <% end %>
            </div>
          <% else %>
            <!-- 顧客用ヘッター -->
            <div class="col">
              <h2><i class="fa-solid fa-user"></i>顧客用ページ</h2>
            </div>
            <div class="col">
              <% if customer_signed_in? %>
                <ul>
                  <li><%= link_to "マイページ", customer_path(current_customer.id) %></li>
                  <li><%= link_to "商品", items_path %></li>
                  <li><%= link_to "カート", cart_items_path %></li>
                  <li><%= link_to "ログアウト", destroy_customer_session_path, method: :delete %></li>
                </ul>
                <h3>ログインユーザ：<%= current_customer.name %></h3>
              <% else %>
                <ul>
                  <li><%= link_to "ルートパス", root_path %></li>
                  <li><%= link_to "ログイン", new_customer_session_path %></li>
                  <li><%= link_to "サインアップ", new_customer_registration_path %></li>
                </ul>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <main>
      <div class="container">
        <%= yield %>
      </div>
    </main>
    <footer></footer>
  </body>
</html>
