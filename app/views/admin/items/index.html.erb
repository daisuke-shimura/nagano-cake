<h1>Admin::Items#index</h1>
<p><%= link_to "商品登録", new_admin_item_path %></p>
<% @items.each do |item| %>
  <%= link_to admin_item_path(item.id) do %>
    <%= item.name %>
  <% end %>
  <%= item.price %>
  <br>
<% end %>
